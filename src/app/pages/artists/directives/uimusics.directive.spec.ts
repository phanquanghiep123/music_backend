import { UimusicsDirective } from './uimusics.directive';

describe('UimusicsDirective', () => {
  it('should create an instance', () => {
    const directive = new UimusicsDirective();
    expect(directive).toBeTruthy();
  });
});
